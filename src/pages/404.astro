---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Página no encontrada | OCR Danger Extreme" description="La página que buscas no existe. Vuelve al inicio de OCR Danger Extreme.">
  <main class="min-h-screen flex flex-col items-center justify-center px-4 splatter-bg noise-overlay relative overflow-hidden">
    <!-- Partículas decorativas -->
    <div class="absolute inset-0 overflow-hidden pointer-events-none" aria-hidden="true">
      <div class="absolute top-1/4 left-1/4 w-2 h-2 bg-danger-red rounded-full animate-ping"></div>
      <div class="absolute bottom-1/3 right-1/4 w-2 h-2 bg-danger-yellow rounded-full animate-ping" style="animation-delay: 1s;"></div>
    </div>

    <div class="relative z-10 text-center max-w-2xl mx-auto">
      <!-- Código de error -->
      <div class="mb-8">
        <span class="text-[8rem] md:text-[12rem] font-black leading-none block">
          <span class="text-danger-yellow">4</span>
          <span class="text-danger-red">0</span>
          <span class="text-danger-yellow">4</span>
        </span>
      </div>

      <!-- Icono de peligro -->
      <div class="mb-6 animate-bounce">
        <span class="text-6xl" role="img" aria-label="Señal de peligro">⚠️</span>
      </div>

      <!-- Mensaje -->
      <h1 class="text-2xl md:text-4xl font-black mb-4">
        <span class="text-white">¡TERRENO </span>
        <span class="text-danger-red">PELIGROSO</span>
        <span class="text-white">!</span>
      </h1>
      
      <p class="text-gray-400 text-lg mb-8">
        Esta ruta no existe. Parece que te has perdido en el recorrido...
      </p>

      <!-- Decoración -->
      <div class="flex items-center justify-center gap-4 mb-8" aria-hidden="true">
        <div class="h-[2px] w-16 md:w-24 bg-gradient-to-r from-transparent to-danger-yellow"></div>
        <span class="text-danger-red text-2xl">☠</span>
        <div class="h-[2px] w-16 md:w-24 bg-gradient-to-l from-transparent to-danger-red"></div>
      </div>

      <!-- CTA -->
      <a 
        href="/" 
        class="btn-danger inline-flex items-center gap-3 text-lg px-8 py-4 group"
        aria-label="Volver al inicio"
      >
        <svg class="w-5 h-5 transform group-hover:-translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
        </svg>
        <span>VOLVER AL INICIO</span>
      </a>

      <!-- Mensaje adicional -->
      <p class="text-gray-600 text-sm mt-8 uppercase tracking-widest">
        El desafío te espera en la línea de salida
      </p>
    </div>
  </main>
</Layout>
