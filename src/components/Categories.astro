---
const categories = [
  {
    name: 'ELITE',
    distance: '6km',
    description: 'Los más duros. Sin piedad. Sin excusas.',
    color: 'red',
    icon: '🏆'
  },
  {
    name: 'PRE-ELITE',
    distance: '6km',
    description: 'El primer paso hacia la gloria extrema.',
    color: 'yellow',
    icon: '🔥'
  },
  {
    name: 'POPULAR INDIVIDUAL',
    distance: '6km',
    description: 'Tú contra el mundo. Demuestra de qué estás hecho.',
    color: 'red',
    icon: '💪'
  },
  {
    name: 'POPULAR PAREJAS',
    distance: '6km',
    description: 'Dos guerreros, un objetivo: sobrevivir juntos.',
    color: 'yellow',
    icon: '🤝'
  }
];

const currentYear = new Date().getFullYear();
---

<section class="py-24 px-4 relative overflow-hidden bg-black" aria-labelledby="categories-title">
  <!-- Efecto de fondo -->
  <div class="absolute inset-0" aria-hidden="true">
    <div class="absolute inset-0 bg-[radial-gradient(ellipse_at_center,_var(--tw-gradient-stops))] from-danger-red/5 via-transparent to-transparent"></div>
  </div>

  <div class="max-w-4xl mx-auto relative z-10">
    <!-- Título -->
    <header class="text-center mb-16">
      <h2 id="categories-title" class="font-black text-4xl md:text-5xl lg:text-6xl">
        <span class="text-danger-yellow">ELIGE</span>
        <span class="text-white"> TU </span>
        <span class="text-danger-red">BATALLA</span>
      </h2>
      <p class="text-gray-500 mt-4 uppercase tracking-widest text-sm">4 Categorías • 1 Destino: La línea de meta</p>
    </header>

    <!-- Lista de categorías -->
    <ul class="space-y-4" role="list" aria-label="Categorías disponibles">
      {categories.map((category, index) => (
        <li class="group relative overflow-hidden">
          <!-- Fondo hover -->
          <div class={`absolute inset-0 bg-gradient-to-r ${category.color === 'red' ? 'from-danger-red/20' : 'from-danger-yellow/20'} to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300`} aria-hidden="true"></div>

          <article class={`relative flex items-center gap-4 md:gap-6 p-6 border-l-4 ${category.color === 'red' ? 'border-danger-red' : 'border-danger-yellow'} bg-gradient-to-r from-gray-900/80 to-transparent rounded-r-lg transition-all duration-300 group-hover:border-l-8`}>

            <!-- Número -->
            <span class="hidden md:block text-6xl font-black text-gray-800 group-hover:text-gray-700 transition-colors select-none" aria-hidden="true">
              0{index + 1}
            </span>

            <!-- Icono -->
            <span class="text-4xl md:text-5xl transform group-hover:scale-125 transition-transform duration-300" role="img" aria-label={category.name}>
              {category.icon}
            </span>

            <!-- Info -->
            <div class="flex-1">
              <div class="flex items-center gap-3 mb-1">
                <h3 class={`text-xl md:text-2xl font-black ${category.color === 'red' ? 'text-danger-red' : 'text-danger-yellow'} group-hover:tracking-wider transition-all`}>
                  {category.name}
                </h3>
                <span class="text-xs px-2 py-1 bg-white/10 rounded text-gray-400 font-bold">
                  {category.distance}
                </span>
              </div>
              <p class="text-gray-400 text-sm md:text-base">{category.description}</p>
            </div>

            <!-- Flecha -->
            <svg class="w-6 h-6 text-gray-600 group-hover:text-white group-hover:translate-x-2 transition-all" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
            </svg>
          </article>
        </li>
      ))}
    </ul>

    <!-- CTA Final -->
    <div class="text-center mt-20">
      <div class="inline-block">
        <p class="text-gray-400 mb-6 text-xl">
          ¿Tienes lo que se necesita para ser un
          <span class="text-danger-red font-black">SURVIVOR</span>?
        </p>

<!-- BOTÓN DE INSCRIPCIÓN (deshabilitado temporalmente)
        <a
          href="https://sportmaniacs.com"
          target="_blank"
          rel="noopener noreferrer"
          class="btn-danger inline-block text-2xl px-16 py-6 relative overflow-hidden group"
          aria-label="Inscríbete ya en OCR Danger Extreme (abre en nueva ventana)"
        >
          <span class="relative z-10 flex items-center gap-4">
            <span>INSCRÍBETE YA</span>
            <span class="text-3xl group-hover:animate-pulse" aria-hidden="true">☠</span>
          </span>
        </a>

        <p class="text-gray-700 text-xs mt-6 uppercase tracking-widest">
          Plazas limitadas • No hay vuelta atrás
        </p>
-->
      </div>
    </div>
  </div>
</section>

<!-- Footer -->
<footer class="bg-black py-12 px-4 border-t border-gray-900 relative" role="contentinfo">
  <div class="absolute top-0 left-0 right-0 h-px bg-gradient-to-r from-transparent via-danger-red/50 to-transparent" aria-hidden="true"></div>

  <div class="max-w-4xl mx-auto">
    <div class="flex flex-col md:flex-row items-center justify-between gap-6">
      <!-- Logo pequeño -->
      <div class="flex items-center gap-3">
        <img
          src="/logo.jpg"
          alt="Logo OCR Danger Extreme"
          width="48"
          height="48"
          loading="lazy"
          decoding="async"
          class="w-12 h-12 rounded-full opacity-80"
          style="mix-blend-mode: screen;"
        />
        <div>
          <p class="font-black text-sm">
            <span class="text-danger-yellow">DANGER</span>
            <span class="text-danger-red">EXTREME</span>
          </p>
          <p class="text-gray-600 text-xs">OCR 2026</p>
        </div>
      </div>

      <!-- Info -->
      <address class="text-center md:text-right not-italic">
        <p class="text-gray-500 text-sm">
          Caleta de Fuste, Antigua, Fuerteventura
        </p>
        <p class="text-gray-700 text-xs mt-1">
          © {currentYear} OCR Danger Extreme. Todos los derechos reservados.
        </p>
      </address>
    </div>

    <!-- Línea final -->
    <div class="mt-8 pt-6 border-t border-gray-900 text-center">
      <p class="text-gray-800 text-xs uppercase tracking-[0.3em]">
        Prepárate para sufrir • Prepárate para ganar
      </p>
      <p class="text-gray-700 text-xs mt-4">
        Desarrollado por <a href="https://nakytec.com" target="_blank" rel="noopener noreferrer" class="text-danger-yellow hover:text-danger-red transition-colors">Nakytec</a>
      </p>
    </div>
  </div>
</footer>
